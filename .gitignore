# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (only refernce example env file in the repo)
.env*
! .env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# AI-instruction files — PRIVATE to this portfolio project, never published to the public
# repo (they're uploaded to the chat knowledge base for AI context instead). Covers the whole
# .claude/ dir (rules + local settings) and the CLAUDE.md context file.
.claude/
CLAUDE.md